()=>{const d=window.gameDebug,s=d.sim,z={throttle:0,steer:0,brake:false};d.manual(true);const result={length:d.COURSE_LENGTH};
s.start(false,'marksman');s.bots.forEach(b=>b.paused=true);s.markYaw=0;const before=s.markEye();for(let i=0;i<60;i++)s.step({...z,throttle:1});const after=s.markEye();for(let i=0;i<900;i++)s.step({...z,steer:1});result.walk={before,after,edge:s.markEye(),radius:Math.hypot(s.markEye().x,s.markEye().z)};
s.cooldown=0;s.fireMarksman({x:0,y:1,z:0});result.ammoAfterShot=s.ammo.sniper;result.reloadStarted=s.requestReload();result.reloadBlocksFire=!s.fireMarksman({x:0,y:1,z:0});s.paused=true;const timer=s.reloadTime;s.step(z);result.reloadPause=s.reloadTime===timer;s.paused=false;for(let i=0;i<120;i++)s.step(z);result.reloadAmmo=s.ammo.sniper;s.attackType='rocket';s.cooldown=0;s.fireMarksman({x:0,y:1,z:0});for(let i=0;i<300;i++)s.step(z);result.autoRocketReload=s.ammo.rocket;
for(const explosive of [false,true]){s.start(true);s.mount();s.invulnerable=0;const b=s.barrels.find(b=>b.explosive===explosive),p=b.body.translation(),t=d.closest(p).t,f=d.frame(t);s.yaw=f.yaw;s.body.setTranslation({x:p.x-f.forward.x*5,y:25,z:p.z-f.forward.z*5},true);s.body.setLinvel({x:f.forward.x*14,y:0,z:f.forward.z*14},true);const initial={...p};for(let i=0;i<45;i++){s.step({...z,throttle:1});if(s.phase==='dead')break;}result[explosive?'fuel':'steel']={phase:s.phase,hits:s.barrelHits,explosions:s.barrelExplosions,active:b.active,moved:Math.hypot(b.body.translation().x-initial.x,b.body.translation().z-initial.z)};}
s.start(true);s.mount();const pad=d.boosts[2],f=d.frame(pad.t);s.yaw=f.yaw;s.body.setTranslation({...f.p,y:25},true);s.body.setLinvel({x:f.forward.x*25,y:0,z:f.forward.z*25},true);let max=0;for(let i=0;i<80;i++){s.step({...z,throttle:1});max=Math.max(max,s.speed);}result.boost={count:s.boostCount,max};
result.routes=[];
for(const r of d.routes){s.start(true);s.mount();s.checkpoint=d.checkpoints.filter(t=>t<r.start).length;const p=r.points[3],q=r.points[4];s.yaw=Math.atan2(q.x-p.x,q.z-p.z);s.body.setTranslation({...p,y:25},true);let last=0,minY=99;
 for(let i=0;i<6500;i++){const p=s.position();let best=Infinity,index=0;r.points.forEach((q,j)=>{const dist=Math.hypot(p.x-q.x,p.z-q.z);if(dist<best){best=dist;index=j;}});last=index;const q=r.points[Math.min(r.points.length-1,index+3)],heading=Math.atan2(q.x-p.x,q.z-p.z),e=Math.atan2(Math.sin(heading-s.yaw),Math.cos(heading-s.yaw));s.step({throttle:s.speed>6?-1:1,steer:Math.max(-1,Math.min(1,-e*1.3)),brake:false});minY=Math.min(minY,s.position().y);if(s.phase==='dead'||index>=r.points.length-4)break;}
 result.routes.push({id:r.id,checkpoint:s.checkpoint,expected:d.checkpoints.filter(t=>t<r.end).length,phase:s.phase,last,total:r.points.length,minY});}
s.start(true);s.mount();result.assets=d.gameAudio.diagnostics();return result;}
